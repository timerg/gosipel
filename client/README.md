# Llama Client

A chat interface for communicating with an AI endpoint, built with Next.js and React.

## Getting Started

Install dependencies and run the development server:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.
